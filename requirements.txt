# Core
fastapi>=0.95,<1.0
uvicorn[standard]>=0.22.0,<1.0
starlette>=0.27,<1.0

# Templating & static files
jinja2>=3.1
aiofiles>=23.1

# File uploads / form parsing
python-multipart>=0.0.6

# Environment / config
python-dotenv>=1.0.0

# Security / Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Database (choose the DB driver that fits your DB)
SQLAlchemy>=1.4,<3.0
databases>=0.6.0      # optional async DB layer (if used)
asyncpg>=0.27.0       # PostgreSQL async driver (optional)

# HTTP client / testing
httpx>=0.24.0

# Testing
pytest>=7.2.0
pytest-asyncio>=0.21.0

# Optional admin UI packages (uncomment if you want a packaged admin)
# fastapi-admin>=0.3.0
# fastapi-admin2>=0.1.0
